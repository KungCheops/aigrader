# Scaffold for solution to DIT 852, Programming task 3
#

def process(s):
    # Process string to create a data structure which helps you to
    # determine a list of all the positions where a character appears
    # in a string s
    #
    # Insert your code below

    return ds
    
def occurrences(ds, queries):
    # For each character c in queries return a list of all the positions
    # where c appears in the string s using ds only (do not use s)
    #
    # Insert your code below
        
    return result
    
    

def run(s, query):
    # Do not change this function
    return occurrences(process(s), query)


# The following is called if you execute the script from the commandline
# e.g. with python solution.py
if __name__ == "__main__":
    assert run('mississippi', ['s','i']) == [[2, 3, 5, 6], [1, 4, 7, 10]]
    


    
